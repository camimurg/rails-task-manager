<h1>My Tasks</h1>
<br>
<% @tasks.each do |task|  %>
  <div>
    <input id="task" type="checkbox" <%= "checked=''" if task.completed == true %>>
    <label for="task"> <%= link_to task.title.capitalize, task_path(task) %> </label>
    <%= link_to "🗑", task_path(task), method: :delete %>
  </div>
<% end %>
<br>
<%= link_to '+ Create a new task', "/tasks/new" %>
